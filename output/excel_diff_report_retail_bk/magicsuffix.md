# Differences for magicsuffix.txt

*Key column used: `Name`*

## Added Rows (2)
- of lower resist
- of townportal
## Modified Rows (149)
### of absorption
- `mod1code`: <code>red-dmg</code> (Old) &rarr; <code>red-dmg</code><ins><code>%</code></ins> (New)
- `mod1min`: <del><code>3</code></del> (Old) &rarr; <ins><code>6</code></ins> (New)
- `mod1max`: <del><code>3</code></del> (Old) &rarr; <ins><code>10</code></ins> (New)
### of acceleration
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `level`: <del><code>51</code></del> (Old) &rarr; <ins><code>65</code></ins> (New)
- `levelreq`: <del><code>43</code></del> (Old) &rarr; <ins><code>57</code></ins> (New)
### of acidity
- `multiply`:  (Old) &rarr; <ins><code>0</code></ins> (New)
- `add`:  (Old) &rarr; <ins><code>0</code></ins> (New)
### of amplify damage
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>22</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>22</code></ins> (New)
### of anima
- `mod1code`: <code>red-dmg</code> (Old) &rarr; <code>red-dmg</code><ins><code>%</code></ins> (New)
- `mod1min`: <del><code>8</code></del> (Old) &rarr; <ins><code>16</code></ins> (New)
- `mod1max`: <del><code>15</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `itype3`: <del><code>circ</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of apocalypse
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `itype1`: <del><code>glov</code></del> (Old) &rarr; <ins><code>circ</code></ins> (New)
### of armageddon
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of attraction
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of bashing
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of battle command
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of battle cry
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of battle orders
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of blazing
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of blessed hammers
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of blizzards
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of blood golem summoning
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of bone armor
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of bone imprisonment
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of bone spears
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of bone spirits
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of bone walls
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of carnage
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### of chain lightning
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of charged bolts
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of charged shield
- `level`: <del><code>26</code></del> (Old) &rarr; <ins><code>45</code></ins> (New)
- `levelreq`: <del><code>19</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1min`: <del><code>14</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1max`: <del><code>5</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
### of charged strike
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of chilling armor
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of clay golem summoning
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of cold arrows
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of concentration
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of conductivity
- `multiply`:  (Old) &rarr; <ins><code>0</code></ins> (New)
- `add`:  (Old) &rarr; <ins><code>0</code></ins> (New)
### of confusion
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of conversion
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of coolness
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### of corpse explosions
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of cyclone armor
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of damage amplification
- `level`: <del><code>3</code></del> (Old) &rarr; <ins><code>60</code></ins> (New)
- `levelreq`:  (Old) &rarr; <ins><code>80</code></ins> (New)
- `group`: <del><code>44</code></del> (Old) &rarr; <ins><code>45</code></ins> (New)
- `mod1code`: <del><code>hit</code></del><code>-skill</code> (Old) &rarr; <ins><code>kill</code></ins><code>-skill</code> (New)
- `mod1min`: <del><code>5</code></del> (Old) &rarr; <ins><code>24</code></ins> (New)
- `mod1max`: <del><code>1</code></del> (Old) &rarr; <ins><code>22</code></ins> (New)
- `itype1`: <del><code>weap</code></del> (Old) &rarr; <ins><code>miss</code></ins> (New)
- `itype2`:  (Old) &rarr; <ins><code>mele</code></ins> (New)
- `etype1`: <del><code>orb</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of decrepification
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of dim vision
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of enchantment
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-10</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of energy shield
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of eruption
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of exploding arrows
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of fending
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of fervor
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### of fire arrows
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of fire balls
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of fire bolts
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of fire golem summoning
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of fire walls
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of firebolts
- `level`: <del><code>20</code></del> (Old) &rarr; <ins><code>45</code></ins> (New)
- `levelreq`: <del><code>15</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1min`: <del><code>10</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1max`: <del><code>4</code></del> (Old) &rarr; <ins><code>30</code></ins> (New)
### of firestorms
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of fist of the heavens
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of force
- `multiply`:  (Old) &rarr; <ins><code>0</code></ins> (New)
- `add`:  (Old) &rarr; <ins><code>0</code></ins> (New)
### of freezing arrows
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of frost novas
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of frost shield
- `level`: <del><code>12</code></del> (Old) &rarr; <ins><code>25</code></ins> (New)
- `levelreq`: <del><code>9</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1min`: <del><code>5</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1max`: <del><code>3</code></del> (Old) &rarr; <ins><code>30</code></ins> (New)
### of frozen armor
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of frozen orbs
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of glacial spikes
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of grim ward
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of guarding
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
- `mod1min`: <del><code>3</code></del> (Old) &rarr; <ins><code>6</code></ins> (New)
- `mod1max`: <del><code>3</code></del> (Old) &rarr; <ins><code>10</code></ins> (New)
### of guided arrows
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of health
- `mod1max`: <del><code>1</code></del> (Old) &rarr; <ins><code>5</code></ins> (New)
### of holy bolts
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of honor
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
- `rare`: <del><code>1</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of hope
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### of howling
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of hurricane
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of hydra shield
- `levelreq`: <del><code>30</code></del> (Old) &rarr; <ins><code>40</code></ins> (New)
- `mod1min`: <del><code>10</code></del> (Old) &rarr; <ins><code>33</code></ins> (New)
- `mod1max`: <del><code>3</code></del> (Old) &rarr; <ins><code>40</code></ins> (New)
### of hydras
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of ice arrows
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of ice blasts
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of ice bolts
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of icebolt
- `level`: <del><code>6</code></del> (Old) &rarr; <ins><code>25</code></ins> (New)
- `levelreq`: <del><code>4</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1min`: <del><code>5</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1max`: <del><code>3</code></del> (Old) &rarr; <ins><code>35</code></ins> (New)
### of immolating arrows
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of impaling strike
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of inner sight
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of iron golem creation
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of iron maiden
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of item finding
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of jabbing
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of kindling
- `multiply`:  (Old) &rarr; <ins><code>0</code></ins> (New)
- `add`:  (Old) &rarr; <ins><code>0</code></ins> (New)
### of lethargy
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of life everlasting
- `mod1min`: <del><code>10</code></del> (Old) &rarr; <ins><code>13</code></ins> (New)
### of life tap
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of light
- `mod1code`: <del><code>light</code></del> (Old) &rarr; <ins><code>att</code></ins> (New)
- `mod1min`: <del><code>1</code></del> (Old) &rarr; <ins><code>10</code></ins> (New)
- `mod1max`: <del><code>1</code></del> (Old) &rarr; <ins><code>10</code></ins> (New)
- `mod2code`: <del><code>att</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `mod2min`: <del><code>10</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `mod2max`: <del><code>10</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of lightning fury
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of lightning javelin
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of lightning strike
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of lower resistance
- `level`: <del><code>72</code></del> (Old) &rarr; <ins><code>85</code></ins> (New)
- `levelreq`: <del><code>72</code></del> (Old) &rarr; <ins><code>80</code></ins> (New)
- `class`: <del><code>nec</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `classlevelreq`: <del><code>30</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `group`: <del><code>44</code></del> (Old) &rarr; <ins><code>45</code></ins> (New)
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>hit-skill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>11</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>22</code></ins> (New)
- `itype1`: <del><code>staf</code></del> (Old) &rarr; <ins><code>miss</code></ins> (New)
- `itype2`:  (Old) &rarr; <ins><code>mele</code></ins> (New)
### of luck
- `itype3`: <del><code>circ</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of magic arrows
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of malice
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
### of meteors
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of miasma bolt
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of molten boulders
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of multiple shot
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of negation
- `mod1min`: <del><code>4</code></del> (Old) &rarr; <ins><code>11</code></ins> (New)
- `mod1max`: <del><code>6</code></del> (Old) &rarr; <ins><code>15</code></ins> (New)
### of nova
- `level`: <del><code>28</code></del> (Old) &rarr; <ins><code>40</code></ins> (New)
- `levelreq`: <del><code>21</code></del> (Old) &rarr; <ins><code>40</code></ins> (New)
- `mod1min`: <del><code>12</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1max`: <del><code>4</code></del> (Old) &rarr; <ins><code>35</code></ins> (New)
### of nova shield
- `level`: <del><code>38</code></del> (Old) &rarr; <ins><code>45</code></ins> (New)
- `levelreq`: <del><code>28</code></del> (Old) &rarr; <ins><code>20</code></ins> (New)
- `mod1min`: <del><code>14</code></del> (Old) &rarr; <ins><code>33</code></ins> (New)
- `mod1max`: <del><code>5</code></del> (Old) &rarr; <ins><code>40</code></ins> (New)
### of novas
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of numbing
- `multiply`:  (Old) &rarr; <ins><code>0</code></ins> (New)
- `add`:  (Old) &rarr; <ins><code>0</code></ins> (New)
### of plague jab
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of poison dagger
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of poison explosion
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of poison jab
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of poison novas
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of potion finding
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of power strike
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of protection
- `mod1code`: <code>red-dmg</code> (Old) &rarr; <code>red-dmg</code><ins><code>%</code></ins> (New)
- `mod1min`: <del><code>2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>2</code></del> (Old) &rarr; <ins><code>5</code></ins> (New)
### of radiance
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
- `mod1code`: <del><code>light</code></del> (Old) &rarr; <ins><code>att</code></ins> (New)
- `mod1min`: <del><code>3</code></del> (Old) &rarr; <ins><code>30</code></ins> (New)
- `mod1max`: <del><code>3</code></del> (Old) &rarr; <ins><code>30</code></ins> (New)
- `mod2code`: <del><code>att</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `mod2min`: <del><code>30</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `mod2max`: <del><code>30</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of raise skeletal mages
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of raise skeletons
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of rancor
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of razors
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
### of resistance
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### of revivification
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of sacrifice
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of shiver armor
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of shouting
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of slow missiles
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of spikes
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
### of static field
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of stunning
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of swords
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
### of taunting
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of teeth
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-6</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of telekinesis
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of terror
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of thaumaturgy
- `multiply`:  (Old) &rarr; <ins><code>0</code></ins> (New)
- `add`:  (Old) &rarr; <ins><code>0</code></ins> (New)
### of the apprentice
- `itype1`: <del><code>rod</code></del> (Old) &rarr; <ins><code>shld</code></ins> (New)
- `itype6`:  (Old) &rarr; <ins><code>glov</code></ins> (New)
- `itype7`:  (Old) &rarr; <ins><code>tors</code></ins> (New)
- `etype1`:  (Old) &rarr; <ins><code>rod</code></ins> (New)
### of the bear
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
- `rare`: <del><code>1</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of the colosuss
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### of the magus
- `itype1`: <del><code>rod</code></del> (Old) &rarr; <ins><code>staf</code></ins> (New)
- `itype4`:  (Old) &rarr; <ins><code>glov</code></ins> (New)
- `itype5`:  (Old) &rarr; <ins><code>shld</code></ins> (New)
- `itype6`:  (Old) &rarr; <ins><code>tors</code></ins> (New)
- `etype2`:  (Old) &rarr; <ins><code>rod</code></ins> (New)
### of the sentinel
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
- `mod1min`: <del><code>2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>2</code></del> (Old) &rarr; <ins><code>5</code></ins> (New)
### of the squid
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### of the sun
- `mod1code`: <del><code>light</code></del> (Old) &rarr; <ins><code>att%</code></ins> (New)
- `mod1min`: <del><code>5</code></del> (Old) &rarr; <ins><code>10</code></ins> (New)
- `mod1max`: <del><code>5</code></del> (Old) &rarr; <ins><code>10</code></ins> (New)
- `mod2code`: <del><code>att%</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `mod2min`: <del><code>5</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `mod2max`: <del><code>5</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of the whale
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### of thunder storm
- `spawnable`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of tornado
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-2</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of traveling
- `rare`:  (Old) &rarr; <ins><code>1</code></ins> (New)
- `level`: <del><code>65</code></del> (Old) &rarr; <ins><code>51</code></ins> (New)
- `levelreq`: <del><code>57</code></del> (Old) &rarr; <ins><code>45</code></ins> (New)
- `mod1min`: <del><code>30</code></del> (Old) &rarr; <ins><code>35</code></ins> (New)
- `mod1max`: <del><code>30</code></del> (Old) &rarr; <ins><code>35</code></ins> (New)
- `mod2code`: <del><code>stamdrain</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `mod2min`: <del><code>80</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `mod2max`: <del><code>90</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### of twister
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of vengeance
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of volcano
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-3</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of war cry
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-1</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of warding
- `spawnable`: <del><code>1</code></del> (Old) &rarr; <ins><code>0</code></ins> (New)
### of weaken
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-5</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### of zeal
- `mod1code`: <del><code>charged</code></del> (Old) &rarr; <ins><code>oskill</code></ins> (New)
- `mod1min`: <del><code>-20</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
- `mod1max`: <del><code>-4</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
