# Differences for magicsuffix.txt

*Key column used: `Name`*

## Added Rows (2)
- of lower resist
- of townportal
## Modified Rows (149)
### of absorption
- `mod1code`: <strong><code>red-dmg</code></strong> (Old) &rarr; <strong><code>red-dmg%</code></strong> (New)
- `mod1min`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>6</code></strong> (New)
- `mod1max`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>10</code></strong> (New)
### of acceleration
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `level`: <strong><code>51</code></strong> (Old) &rarr; <strong><code>65</code></strong> (New)
- `levelreq`: <strong><code>43</code></strong> (Old) &rarr; <strong><code>57</code></strong> (New)
### of acidity
- `multiply`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `add`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of amplify damage
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>22</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>22</code></strong> (New)
### of anima
- `mod1code`: <strong><code>red-dmg</code></strong> (Old) &rarr; <strong><code>red-dmg%</code></strong> (New)
- `mod1min`: <strong><code>8</code></strong> (Old) &rarr; <strong><code>16</code></strong> (New)
- `mod1max`: <strong><code>15</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `itype3`: <strong><code>circ</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of apocalypse
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `itype1`: <strong><code>glov</code></strong> (Old) &rarr; <strong><code>circ</code></strong> (New)
### of armageddon
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of attraction
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of bashing
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of battle command
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of battle cry
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of battle orders
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of blazing
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of blessed hammers
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of blizzards
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of blood golem summoning
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of bone armor
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of bone imprisonment
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of bone spears
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of bone spirits
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of bone walls
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of carnage
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of chain lightning
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of charged bolts
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of charged shield
- `level`: <strong><code>26</code></strong> (Old) &rarr; <strong><code>45</code></strong> (New)
- `levelreq`: <strong><code>19</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1min`: <strong><code>14</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1max`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
### of charged strike
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of chilling armor
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of clay golem summoning
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of cold arrows
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of concentration
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of conductivity
- `multiply`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `add`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of confusion
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of conversion
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of coolness
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of corpse explosions
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of cyclone armor
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of damage amplification
- `level`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>60</code></strong> (New)
- `levelreq`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>80</code></strong> (New)
- `group`: <strong><code>44</code></strong> (Old) &rarr; <strong><code>45</code></strong> (New)
- `mod1code`: <strong><code>hit-skill</code></strong> (Old) &rarr; <strong><code>kill-skill</code></strong> (New)
- `mod1min`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>24</code></strong> (New)
- `mod1max`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>22</code></strong> (New)
- `itype1`: <strong><code>weap</code></strong> (Old) &rarr; <strong><code>miss</code></strong> (New)
- `itype2`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>mele</code></strong> (New)
- `etype1`: <strong><code>orb</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of decrepification
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of dim vision
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of enchantment
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-10</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of energy shield
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of eruption
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of exploding arrows
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of fending
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of fervor
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of fire arrows
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of fire balls
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of fire bolts
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of fire golem summoning
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of fire walls
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of firebolts
- `level`: <strong><code>20</code></strong> (Old) &rarr; <strong><code>45</code></strong> (New)
- `levelreq`: <strong><code>15</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1min`: <strong><code>10</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1max`: <strong><code>4</code></strong> (Old) &rarr; <strong><code>30</code></strong> (New)
### of firestorms
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of fist of the heavens
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of force
- `multiply`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `add`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of freezing arrows
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of frost novas
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of frost shield
- `level`: <strong><code>12</code></strong> (Old) &rarr; <strong><code>25</code></strong> (New)
- `levelreq`: <strong><code>9</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1min`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1max`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>30</code></strong> (New)
### of frozen armor
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of frozen orbs
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of glacial spikes
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of grim ward
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of guarding
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `mod1min`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>6</code></strong> (New)
- `mod1max`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>10</code></strong> (New)
### of guided arrows
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of health
- `mod1max`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>5</code></strong> (New)
### of holy bolts
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of honor
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `rare`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of hope
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of howling
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of hurricane
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of hydra shield
- `levelreq`: <strong><code>30</code></strong> (Old) &rarr; <strong><code>40</code></strong> (New)
- `mod1min`: <strong><code>10</code></strong> (Old) &rarr; <strong><code>33</code></strong> (New)
- `mod1max`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>40</code></strong> (New)
### of hydras
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of ice arrows
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of ice blasts
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of ice bolts
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of icebolt
- `level`: <strong><code>6</code></strong> (Old) &rarr; <strong><code>25</code></strong> (New)
- `levelreq`: <strong><code>4</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1min`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1max`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>35</code></strong> (New)
### of immolating arrows
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of impaling strike
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of inner sight
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of iron golem creation
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of iron maiden
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of item finding
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of jabbing
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of kindling
- `multiply`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `add`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of lethargy
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of life everlasting
- `mod1min`: <strong><code>10</code></strong> (Old) &rarr; <strong><code>13</code></strong> (New)
### of life tap
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of light
- `mod1code`: <strong><code>light</code></strong> (Old) &rarr; <strong><code>att</code></strong> (New)
- `mod1min`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>10</code></strong> (New)
- `mod1max`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>10</code></strong> (New)
- `mod2code`: <strong><code>att</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `mod2min`: <strong><code>10</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `mod2max`: <strong><code>10</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of lightning fury
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of lightning javelin
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of lightning strike
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of lower resistance
- `level`: <strong><code>72</code></strong> (Old) &rarr; <strong><code>85</code></strong> (New)
- `levelreq`: <strong><code>72</code></strong> (Old) &rarr; <strong><code>80</code></strong> (New)
- `class`: <strong><code>nec</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `classlevelreq`: <strong><code>30</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `group`: <strong><code>44</code></strong> (Old) &rarr; <strong><code>45</code></strong> (New)
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>hit-skill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>11</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>22</code></strong> (New)
- `itype1`: <strong><code>staf</code></strong> (Old) &rarr; <strong><code>miss</code></strong> (New)
- `itype2`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>mele</code></strong> (New)
### of luck
- `itype3`: <strong><code>circ</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of magic arrows
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of malice
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of meteors
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of miasma bolt
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of molten boulders
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of multiple shot
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of negation
- `mod1min`: <strong><code>4</code></strong> (Old) &rarr; <strong><code>11</code></strong> (New)
- `mod1max`: <strong><code>6</code></strong> (Old) &rarr; <strong><code>15</code></strong> (New)
### of nova
- `level`: <strong><code>28</code></strong> (Old) &rarr; <strong><code>40</code></strong> (New)
- `levelreq`: <strong><code>21</code></strong> (Old) &rarr; <strong><code>40</code></strong> (New)
- `mod1min`: <strong><code>12</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1max`: <strong><code>4</code></strong> (Old) &rarr; <strong><code>35</code></strong> (New)
### of nova shield
- `level`: <strong><code>38</code></strong> (Old) &rarr; <strong><code>45</code></strong> (New)
- `levelreq`: <strong><code>28</code></strong> (Old) &rarr; <strong><code>20</code></strong> (New)
- `mod1min`: <strong><code>14</code></strong> (Old) &rarr; <strong><code>33</code></strong> (New)
- `mod1max`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>40</code></strong> (New)
### of novas
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of numbing
- `multiply`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `add`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of plague jab
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of poison dagger
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of poison explosion
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of poison jab
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of poison novas
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of potion finding
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of power strike
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of protection
- `mod1code`: <strong><code>red-dmg</code></strong> (Old) &rarr; <strong><code>red-dmg%</code></strong> (New)
- `mod1min`: <strong><code>2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>2</code></strong> (Old) &rarr; <strong><code>5</code></strong> (New)
### of radiance
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `mod1code`: <strong><code>light</code></strong> (Old) &rarr; <strong><code>att</code></strong> (New)
- `mod1min`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>30</code></strong> (New)
- `mod1max`: <strong><code>3</code></strong> (Old) &rarr; <strong><code>30</code></strong> (New)
- `mod2code`: <strong><code>att</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `mod2min`: <strong><code>30</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `mod2max`: <strong><code>30</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of raise skeletal mages
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of raise skeletons
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of rancor
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of razors
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of resistance
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of revivification
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of sacrifice
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of shiver armor
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of shouting
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of slow missiles
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of spikes
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of static field
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of stunning
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of swords
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of taunting
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of teeth
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-6</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of telekinesis
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of terror
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of thaumaturgy
- `multiply`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `add`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of the apprentice
- `itype1`: <strong><code>rod</code></strong> (Old) &rarr; <strong><code>shld</code></strong> (New)
- `itype6`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>glov</code></strong> (New)
- `itype7`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>tors</code></strong> (New)
- `etype1`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>rod</code></strong> (New)
### of the bear
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `rare`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of the colosuss
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of the magus
- `itype1`: <strong><code>rod</code></strong> (Old) &rarr; <strong><code>staf</code></strong> (New)
- `itype4`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>glov</code></strong> (New)
- `itype5`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>shld</code></strong> (New)
- `itype6`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>tors</code></strong> (New)
- `etype2`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>rod</code></strong> (New)
### of the sentinel
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
- `mod1min`: <strong><code>2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>2</code></strong> (Old) &rarr; <strong><code>5</code></strong> (New)
### of the squid
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of the sun
- `mod1code`: <strong><code>light</code></strong> (Old) &rarr; <strong><code>att%</code></strong> (New)
- `mod1min`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>10</code></strong> (New)
- `mod1max`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>10</code></strong> (New)
- `mod2code`: <strong><code>att%</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `mod2min`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `mod2max`: <strong><code>5</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of the whale
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of thunder storm
- `spawnable`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of tornado
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-2</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of traveling
- `rare`: <strong><code>empty</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `level`: <strong><code>65</code></strong> (Old) &rarr; <strong><code>51</code></strong> (New)
- `levelreq`: <strong><code>57</code></strong> (Old) &rarr; <strong><code>45</code></strong> (New)
- `mod1min`: <strong><code>30</code></strong> (Old) &rarr; <strong><code>35</code></strong> (New)
- `mod1max`: <strong><code>30</code></strong> (Old) &rarr; <strong><code>35</code></strong> (New)
- `mod2code`: <strong><code>stamdrain</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `mod2min`: <strong><code>80</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
- `mod2max`: <strong><code>90</code></strong> (Old) &rarr; <strong><code>(removed)</code></strong> (New)
### of twister
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of vengeance
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of volcano
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-3</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of war cry
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-1</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of warding
- `spawnable`: <strong><code>1</code></strong> (Old) &rarr; <strong><code>0</code></strong> (New)
### of weaken
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-5</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
### of zeal
- `mod1code`: <strong><code>charged</code></strong> (Old) &rarr; <strong><code>oskill</code></strong> (New)
- `mod1min`: <strong><code>-20</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
- `mod1max`: <strong><code>-4</code></strong> (Old) &rarr; <strong><code>1</code></strong> (New)
