# Differences for properties.txt

*Key column used: `code`*

## Added Columns: `\*Id, uiRangeType`  

## Added Rows (6)
- affix\-rand
- dmg\-mag/lvl
- extra\-skill\-dmg%\-rand
- pierce\-dmg
- war
- war\-tab\-rand
## Removed Rows (7)
- dyecblue1
- dyecred1
- dyedarkpurple1
- dyedarkyellow1
- dyelightgreen1
- dyelightpurple1
- dyeorange1
## Modified Rows (8)
### corruption1
- `\*Tooltip`:  (Old) &rarr; <ins><code>1 through 1000</code></ins> (New)
- `\*Min`:  (Old) &rarr; <ins><code>Min #</code></ins> (New)
- `\*Max`:  (Old) &rarr; <ins><code>Max #</code></ins> (New)
### dyed1
- `\*Min`:  (Old) &rarr; <ins><code>Min %</code></ins> (New)
- `\*Max`:  (Old) &rarr; <ins><code>Max %</code></ins> (New)
### enr%
- `\*Min`: <del><code>Min %</code></del> (Old) &rarr; <ins><code>100</code></ins> (New)
- `\*Max`: <del><code>Max %</code></del> (Old) &rarr; <ins><code>1</code></ins> (New)
### hit\-skill\-splash
- `\*Min`: <del><code>"</code></del><code>%</code><del><code> Chance (If 0, then default to 5)"</code></del> (Old) &rarr; <ins><code>Min </code></ins><code>%</code> (New)
- `\*Max`: <del><code>Skill</code></del><code> </code><del><code>Level</code></del> (Old) &rarr; <ins><code>Max</code></ins><code> </code><ins><code>%</code></ins> (New)
### noconsume
- `\*Enabled`:  (Old) &rarr; <ins><code>1</code></ins> (New)
### pierce\-mag
- `\*Tooltip`: <code>-#% to Enemy Magic  Resistance</code> (Old) &rarr; <code>-#% to Enemy Magic Resistance</code> (New)
### randclassskill3
- `\*Min`: <code>Min </code><del><code>Class ID</code></del> (Old) &rarr; <code>Min </code><ins><code>#</code></ins> (New)
- `\*Max`: <code>Max </code><del><code>Class ID</code></del> (Old) &rarr; <code>Max </code><ins><code>#</code></ins> (New)
- `\*Notes`: <del><code>val1 = # of Skill levels</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
### splash
- `\*Min`: <del><code>100</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
- `\*Max`: <del><code>1</code></del> (Old) &rarr; <ins><code>(removed)</code></ins> (New)
